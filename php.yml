version: "3.9"
services:
  php:
    env_file:
      - config/php.env
    volumes:
      - ./sites:/var/www/html/
      - ~/sites:/sites/
      - tmp:/tmp
      - ./scripts/drush:/usr/bin/drush
    networks:
      - webdev
